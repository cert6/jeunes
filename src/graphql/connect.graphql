query($email:String,$password:String)
{
  jeunes(where:{
    email:$email
    password:$password
  })
  {
     id
    nom
      email
      contact
      password
      profession
      pays
      ville
      localisation
      createdAt
      updatedAt
      active
  }
}